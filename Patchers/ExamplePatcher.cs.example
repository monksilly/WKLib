public class ExamplePatcher
{
    [PatchTarget(typeof(SomeGameClass), "TargetMethod")]
    public class ExamplePatcher : IPatcher
    {
        public static bool Prefix(SomeGameClass __instance)
        {
            Debug.Log("ExamplePatcher Prefix hit in consumer mod.");
            return true;
        }

        public static void Postfix(SomeGameClass __instance)
        {
            Debug.Log("ExamplePatcher Postfix hit in consumer mod.");
        }
    }
}